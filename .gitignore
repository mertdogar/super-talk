node_modules/
dist/
# the plugin's bundle is committed: the Claude Code marketplace clones the repo and never builds.
!packages/plugin/dist/
!packages/plugin/dist/**
*.tsbuildinfo
.DS_Store

# local Claude Code working setup (not part of the product)
.claude/

# super-talk hub SQLite store
*.db
*.db-shm
*.db-wal

# super-talk per-developer runtime state (agent name, last channels)
.super-talk/

# Generated docs
docs/.vitepress/cache/
docs/.vitepress/.temp/
docs/reference/

# impeccable hook cache
.impeccable/
